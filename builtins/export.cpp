#include <builtins/export.h>

#include <cstdlib>
extern char** environ;
// export
void export_env(const std::vector<std::string>& args) {
  switch (args.size()) {
    case 1:
      break;
    case 2:
      break;
  }
}